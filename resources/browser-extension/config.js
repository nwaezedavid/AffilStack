// Where this copy of the extension sends captured pages. The copy people
// download from their AffilStack dashboard has this line rewritten to that
// site's own address; the Chrome Web Store build ships with affilstack.com.
const AFFILSTACK_DEFAULT_API_BASE_URL = 'https://affilstack.com/api';
